# Hyperparameter Tuning Experiment

RandomizedSearchCV was run inside repeated stratified outer folds on the combined report + H5 feature set.

Best tuned model: random_forest

Balanced accuracy: 0.732

Macro F1: 0.716

ROC-AUC: 0.808
